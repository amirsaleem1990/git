# github
cd ~ ; mkdir github ; cd github/ ; git init ; git config --global user.name "amirsaleem1990" ; git config --global user.email "amirsaleem1990@hotmail.com"

1- go to https://github.com/settings/keys and delete existing SSH key
2- ssh-keygen -t rsa -b 4096 -C "amirsaleem1990@hotmail.com"
3- When you're prompted to "Enter a file in which to save the key," press Enter. This accepts the default file location.
4- At the prompt, press Enter
5- eval "$(ssh-agent -s)"
6- ssh-add ~/.ssh/id_rsa
7- sudo apt-get install xclip
8- xclip -sel clip < ~/.ssh/id_rsa.pub
9- go to https://github.com/settings/keys
10- Click New SSH key or Add SSH key
11- In the "Title" field, add a descriptive label for the new key. For example, if you're using a personal Mac, you might call this key "Personal MacBook Air".
12- CTRL+V (ve copied a key in line 8)
13 - Click Add SSH key. 


14- ipython
import os
base = "git@github.com:amirsaleem1990/"
for i in [
"The-analytics-edge.git",
"Linux.git",
"short-codes.git",
"important.git",
"Notes.git",
"Assembly-language-learning.git",
"linux_youtube_downloader.git",
"working.git",
"Data_Cleaning.git",
"R_simple_codes.git",
"Wikipedia_scraping.git"
]:
	os.system("git clone {}{}".format(base, i))
